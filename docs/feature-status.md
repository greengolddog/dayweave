# DayWeave feature status

Last updated: **2026-09-10**

Tracking baseline: the `main` commit containing this ledger revision

This is the maintained implementation ledger for DayWeave. It answers two
questions: what exists in the repository now, and what still has to be added
before the full app is complete.

The authoritative scope remains [product-requirements.md](product-requirements.md),
which derives from the complete [discovery answer ledger](discovery-answers.md).
This file does not weaken or replace any requirement. A row marked implemented
means only the capability described in that row is committed and has automated
coverage; it does **not** mean that every requirement in the related product area
is complete.

## Status meanings

| Status | Meaning |
| --- | --- |
| **Implemented + covered** | The stated slice is committed and exercised by automated tests or a guarded build check. |
| **In progress** | Implementation exists on the active development path, but the slice is not yet fully integrated, verified, and committed. |
| **Planned** | Required by the product specification and still to be built or completed. |
| **External gate** | Code can progress, but final verification needs owner-controlled credentials, infrastructure approval, hardware, or real-life use. |

## Release targets

- macOS: polished native app distributed as a verified local build.
- Android: polished native app distributed as a signed APK, with final testing
  on the owner's Pixel 11.
- Backend: private DayWeave service on an owner-approved Nebius deployment,
  kept within the USD 50/month ceiling.
- Integrations: bidirectional Google Calendar and Google Tasks, embedded Codex,
  and a permissioned MCP/skill path for ChatGPT/Codex suggestions.

## Current implemented feature slices

| Capability available in the repository | Related scope | Evidence |
| --- | --- | --- |
| Encrypted macOS/Android occurrence observations and exact per-instance outboxes, receipt-target catch-up, bounded terminal state installation and safe predecessor migration | Durable native foundation for `HIE-004`, `ROU`, `SYNC` and `SEC` | [Custody contract and verification](routine-occurrence-completion.md#native-encrypted-custody-foundation), [macOS state](../apps/macos/Sources/DayWeaveMac/Models/RoutineOccurrenceState.swift), [Android state](../apps/android/app/src/main/java/com/greengolddog/dayweave/model/RoutineOccurrenceState.kt), and [Room 24 schema](../apps/android/app/schemas/com.greengolddog.dayweave.data.PlannerDatabase/24.json). Full native gates pass 1,100 executed macOS tests (three opt-in skips) and 1,697 Android JVM tests (two opt-in skips), including 17/23 new persistence regressions. The Android migration test compiles but is not yet instrumented. Schema 28 / JSON_V24 preserve existing exact journals; failed writes retain custody, historical receipts cannot replace newer reviews, and changed terminal checkpoints durably require schedule catch-up. Live protected review/replay, reconnect and schedule invalidation wiring, helper-v2 evidence and user controls remain unfinished. |
| Strict macOS/Android recurring-instance review and mutation clients, complete iterative evidence validation, exact historical receipt binding and bounded whole-instance paging | Native wire checkpoint for `HIE-004`, `ROU`, `SYNC` and `SEC` | [Native contract and verification](routine-occurrence-completion.md#native-wire-checkpoint), [shared producer corpus](../fixtures/routine-occurrences/README.md), [macOS transport](../apps/macos/Sources/DayWeaveMac/Store/RoutineOccurrenceAPIClient.swift), and [Android transport](../apps/android/app/src/main/java/com/greengolddog/dayweave/network/RoutineOccurrenceApi.kt). Both platforms consume 48 accepted and 115 rejected synthetic cases with raw numeric tokens preserved. Full native regression gates, the new Rust contract target and warnings-denied Clippy pass. Responses and local page serialization are bounded; Android cancellation retains ownership through body reads. This is not yet native protected controls, encrypted per-instance custody, terminal convergence or qualified local-composition integration. |
| Explicit occurrence-aware local composition protocol v2, current-source/member joining, independent fingerprint/head binding and unchanged v1 compatibility | Shared helper/JNI bridge for `SCH`, `HIE-004` and `ROU` | [Protocol and trust boundary](scheduler-helper.md#occurrence-aware-composition-protocol-v2), [nine helper regressions](../crates/dayweave-scheduler-helper/tests/occurrence_composition.rs), and [opaque Android bridge](../crates/dayweave-android-ffi/src/lib.rs). The shared Rust gate passes 333 tests and warnings-denied Clippy passes. Complete per-instance outcomes preserve optional demand and validate omitted Inbox members; a 5,000-level occurrence runs on a 512-KiB stack. This does not authenticate supplied evidence or enable native local composition: qualified current-source/terminal ledger evidence, encrypted native custody, adapters and installation fences remain unfinished. |
| Publication-qualified recurring Task/Routine member state: immutable complete manifests, reviewed Done/Skipped/reopening and parent policy, exact replay, whole-instance paging, private owner-device API, lifecycle-aware planning/publication and timer-start guards | Server/shared checkpoint for `HIE-004`, `ROU`, `SCH` and `EXE` | [Contract, verification and remaining scope](routine-occurrence-completion.md), [core regressions](../crates/dayweave-core/tests/occurrence_lifecycle.rs), [domain](../server/dayweave-api/src/routine_occurrences/domain.rs), [migration 0036](../server/dayweave-api/migrations/0036_routine_occurrences.sql), [HTTP tests](../server/dayweave-api/tests/routine_occurrence_api.rs), and [PostgreSQL integration](../server/dayweave-api/tests/routine_occurrence_postgres.rs). The shared Rust gate passes 323 tests and the full API gate passes 671 against fresh PostgreSQL with database-only tests enabled and none ignored; workspace warnings-denied Clippy and formatting pass. Completion remains per member/per instance and never mutates the template or cancels unfinished optional work. Positive ledger heads bind private publication v6; legacy v5, first-publication Defer and exact historical receipts remain supported. Native controls/replicas and ledger catch-up, cadence/old-instance retention and full acceptance remain unfinished. |
| Pure required-descendant completion decisions: optional branch cuts, visible-policy modes, retained reopening provenance, qualified occurrence boundaries and iterative deep-forest validation | Engine foundation for `HIE-004` | [Evaluator](../crates/dayweave-core/src/hierarchy_completion.rs), [22 focused regressions](../crates/dayweave-core/tests/hierarchy_completion.rs), and [rules/integration boundary](hierarchy-completion.md). Its initial checkpoint passed 199 core tests and warnings-denied Clippy; current shared verification is recorded above. The pure engine is separate from the authoritative server checkpoint below and does not expose native controls. |
| One-off authoritative parent completion: revision-bound required-edge/manual-policy commands, exact blocker reopening, synchronous direct/proposal/Google writers, provenance-safe undo, permanent replay, startup reconciliation and bounded deep cascades | Server checkpoint for `HIE-004` | [API/release boundary](item-completion-api.md), [migration](../server/dayweave-api/migrations/0035_item_completion.sql), [HTTP tests](../server/dayweave-api/tests/item_completion_api.rs), [deep PostgreSQL tests](../server/dayweave-api/tests/item_completion_postgres.rs), and [proposal/startup tests](../server/dayweave-api/tests/proposal_application_postgres.rs). The prior full server regression passed 626 tests against live PostgreSQL, with warnings-denied Clippy and formatting checks. Coverage includes four 5,000-level completion/reopen cascades, cold bootstrap after more than 20,000 historical changes, real Google timestamp/mapping-CAS behavior and historical-undo schema upgrades. Native integration and bounded service convergence are covered separately below; deep native cascades, recurring-instance integration and owner-device acceptance remain unfinished. |
| macOS and Android completion review, required-edge/manual-policy controls, encrypted exact intent and restart recovery, durable post-receipt catch-up, and policy-qualified Completed-parent child attachment | Native checkpoint for `HIE-004` | [Native contract and verification](item-completion-api.md#native-client-checkpoint), [macOS store](../apps/macos/Sources/DayWeaveMac/Store/ItemCompletionStore.swift), [Android manager](../apps/android/app/src/main/java/com/greengolddog/dayweave/sync/ItemCompletionSyncManager.kt), and [shared wire cases](../fixtures/item-completion/README.md). Full gates pass 1,059 executed macOS tests with warnings denied (three opt-in skips; 1,062 total) and 1,654 Android JVM tests (two opt-in skips; 1,656 total), including the Android composition-clock fix. Android lint has zero errors and both debug APKs build. Synthetic macOS review was previously visually inspected; ten Android UI tests and its Room migration test compile but are not yet instrumented. Global GET review authority is transient; stale/ambiguous requests retain exact custody, receipt settlement waits for terminal canonical hydration, and all completion-derived review/intent remains protected even over a locally public tree. Recurring instances, deeper native/lifecycle coverage and physical acceptance remain open. |
| Real native completion conflict, lost-reply/restart recovery, required/optional cascade hydration and new-child-driven exact ancestor reopening | Controlled convergence checkpoint for `HIE-004` | [Nine-phase gate and coverage boundary](native-completion-convergence.md). One clean command passed all macOS/Android JVM phases against a fresh HTTP/PostgreSQL service, plus final SQL proving four immutable reviewed operations, eight effects and six evaluations. Owned services and retained test runtime were cleaned up. The four-to-five-item scenario uses independent encrypted stores sharing one enrolled synthetic principal; it is not a deep native cascade, physical Room/Keystore test, production TLS/authentication trial or owner acceptance. CI also runs 39 pure scenario/harness tests and 17 mocked runtime-wrapper cases. |
| Deterministic, offline Rust scheduling with hard and soft constraints, task splitting, recurrence materialization, bounded composition, and stable schedule output | `CON`, `SCH`, parts of `HIE` | [`dayweave-core`](../crates/dayweave-core/src/scheduler.rs), [`dayweave-compose`](../crates/dayweave-compose/src/prepare.rs), and their [scheduler tests](../crates/dayweave-core/tests/scheduler.rs) |
| Deep recurring-tree composition without the former 256-level helper limit: iterative traversal, indexed recurring roots/spacing, potential-leaf work accounting and checked aggregate materialization | `HIE-001`, parts of `ROU`, `SCH` and `PERF` | [Core regressions](../crates/dayweave-core/src/recurrence_materialization_limits.rs), [helper-protocol regressions](../crates/dayweave-scheduler-helper/tests/deep_recurrence.rs), and [shared limits](scheduler-helper.md#acceptance-limits). The shared Rust gate passes 302 tests and the full API gate passes 638 against fresh PostgreSQL with all database-only tests enabled and no ignored tests; workspace warnings-denied Clippy and formatting pass. A 5,000-node recurring tree produces the same single leaf block and occurrence identity as a shallow equivalent, including through the helper on a 512-KiB stack and server preview/private publication recomputation. Early habit ownership/carry probes now apply preflight too. Wide executable graphs, oversized recurrence products and malformed topology still fail closed. This is not the occurrence-member completion ledger, native deep-cascade interaction, or physical-device acceptance. |
| Sandboxed, bounded scheduler-helper protocol used by native clients, including strict JSON handling and packaged macOS/Android build paths | `SCH-001`, `MAINT-001`, parts of `PERF` and `SEC` | [`dayweave-scheduler-helper`](../crates/dayweave-scheduler-helper/src/lib.rs), [macOS helper build](../scripts/build-macos-scheduler-helper.sh), and [Android scheduler build](../scripts/build-android-scheduler-library.sh) |
| Canonical item authoring and synchronization, rich scheduling metadata, anchor-safe bounded custom recurrence with canonical storage, recoverable 30-day trash, structural goal/project/task semantics, unlimited-depth cycle prevention, and four dependency types | `DOM`, `HIE`, `GOAL`, parts of `SYNC` and `ROU` | [item API](../server/dayweave-api/src/items), [item API tests](../server/dayweave-api/tests/items_api.rs), [structural domain tests](../crates/dayweave-core/tests/structural_domain.rs), and [item-sync contract](item-sync.md) |
| Historical canonical-authoring receipt recovery without replacing newer item, trash or deletion evidence; exact submitted replay remains distinct from fresh-edit validation | Parts of `SYNC-002`–`SYNC-006` | [Recovery contract](item-sync.md#historical-authoring-receipts), [macOS store regressions](../apps/macos/Tests/DayWeaveMacTests/CanonicalAuthoringHistoricalReceiptTests.swift), [macOS sync regressions](../apps/macos/Tests/DayWeaveMacTests/CanonicalSyncStoreTests.swift), and [Android sync regressions](../apps/android/app/src/test/java/com/greengolddog/dayweave/sync/CanonicalSyncManagerTest.kt). This is a recovery prerequisite, not server parent auto-completion. |
| Bounded cold/current-state item bootstrap with immutable PostgreSQL manifests, fixed-head pagination, recent bodyless trash, terminal-only native installation and ordinary incremental handoff; interrupted snapshots preserve encrypted recovery intent | Parts of `SYNC-001`–`SYNC-006`, prerequisite for `HIE-004` | [Contract and verification](item-sync.md#bounded-current-state-bootstrap), [manifest migration](../server/dayweave-api/migrations/0034_item_bootstrap.sql), [history-heavy PostgreSQL tests](../server/dayweave-api/tests/item_bootstrap_postgres.rs), [real macOS HTTP/store checkpoint](../apps/macos/Tests/DayWeaveMacTests/NativeBootstrapConvergenceTests.swift), and [Android tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/sync/CanonicalSyncManagerTest.kt). Deep native fixtures and a real Mac/loopback service run cover item hydration and encrypted restart, not physical-device acceptance or completion-cascade writers. Upgrade the server before deploying clients that opt into cold bootstrap. |
| Leaf-only flexible demand in the shared scheduler and native readiness/actionability checks, fixed-event interval preservation, no duplicate parent estimates, stale parent-reservation rejection, and iterative duration roll-ups with 5,000-level regression coverage | `HIE-001`–`HIE-003`, parts of `GOAL-001` | [domain](../crates/dayweave-core/src/domain.rs), [scheduler regressions](../crates/dayweave-core/tests/scheduler.rs), [roll-up regressions](../crates/dayweave-core/tests/structural_domain.rs), [macOS first-item tests](../apps/macos/Tests/DayWeaveMacTests/OnboardingFirstItemAnchorTests.swift), [Android first-item tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/model/OnboardingReadinessProofTest.kt), and [scheduling contract](scheduling-api.md). Stored flags and encrypted onboarding designations remain intact; this does not add independent parent progress tracking or a hierarchy browser. |
| Recorded hierarchy summaries on macOS and Android: complete-cache leaf lifecycle counts, separate recurring leaves/fixed events, exact-second rich effort estimates, unknown-estimate counts, parent estimate exclusion, pending-change and privacy safeguards, and search-independent cached presentation | Parts of `HIE-002`–`HIE-003`, `GOAL-001`, and `UX` | [shared reducer](../crates/dayweave-core/src/hierarchy_progress.rs), [parity fixtures](../fixtures/hierarchy-progress/projection-v1.json), [macOS model/tests](../apps/macos/Tests/DayWeaveMacTests/CanonicalHierarchyRollupTests.swift), [Android presentation tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/ui/authoring/CanonicalHierarchyRollupPresentationTest.kt), [Android UI tests](../apps/android/app/src/androidTest/java/com/greengolddog/dayweave/CanonicalHierarchyRollupUiTest.kt), and [contract/verification](hierarchy-progress.md). This read-only slice does not implement weighted/independent progress or automatic completion. |
| Independent item progress on macOS and Android: percentage, self-reported elapsed/remaining time and exact named quantities; parent-own components separate from child totals; revisioned backend storage, immutable replay/audit, encrypted offline journals, reviewed conflicts and privacy-safe recovery | `DOM-004`, independent-component slice of `HIE-003`, parts of `SYNC` and `SEC` | [progress API](../server/dayweave-api/src/item_progress), [PostgreSQL migration](../server/dayweave-api/migrations/0033_item_progress.sql), [shared fixtures](../fixtures/item-progress/README.md), [macOS transport tests](../apps/macos/Tests/DayWeaveMacTests/ItemProgressAPIClientTests.swift), [Android sync tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/sync/ItemProgressSyncManagerTest.kt), and [contract/verification](item-progress.md). [Controlled native transport/store convergence](native-progress-convergence.md) passes offline conflict, lost response and service/native restart checks without changing lifecycle or scheduling demand. Foreground/detail refresh, reconnect hints, stale-editor guards and canonical catch-up have deterministic native coverage; physical-device timing and owner acceptance remain open. |
| Server-authoritative schedule preview/publication replication with revision fencing, immutable published read models, and content-free invalidation streams | `SCH-005`–`SCH-010`, `SYNC-007`–`SYNC-008` | [scheduling API](../server/dayweave-api/src/scheduling), [schedule tests](../server/dayweave-api/tests/schedule_preview.rs), and [scheduling contract](scheduling-api.md). Android now normalizes only newly captured remote/local composition clocks to microseconds while retaining full-resolution local rollback checks and exact saved-request replay. All 150 [sync-manager tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/sync/CanonicalSyncManagerTest.kt) pass, including five focused precision, restart, legacy-custody and rollback regressions. The [real-service convergence gate](native-completion-convergence.md) also passed with a forced sub-microsecond clock through child-authoring preview/publication. This repairs an existing contract; broader feature estimates are unchanged. |
| Cross-device active execution protocol with one active item, pause/resume, timed and indefinite breaks, safe “will do later,” durable replacement schedules, and execution invalidation/catch-up | `EXE-001`, `EXE-003`–`EXE-007`, parts of `NOT-001` | [execution API](../server/dayweave-api/src/execution), [execution tests](../server/dayweave-api/tests/execution_api.rs), and [execution-sync contract](execution-sync.md) |
| Encrypted local planner state on macOS and Android, durable offline mutation journals, idempotent replay, visible reviewed conflicts, credential-generation fencing, and sensitive-item containment | `SYNC-001`–`SYNC-006`, `SEC-006`–`SEC-010` | [macOS persistence](../apps/macos/Sources/DayWeaveMac/Store/EncryptedPlannerPersistence.swift), [Android persistence](../apps/android/app/src/main/java/com/greengolddog/dayweave/data/PlannerStateRepository.kt), and [security model](security.md) |
| Native app lock on both clients, backed by Keychain/Keystore proof, plus privacy boundaries for lock screens, widgets, assistants, and external integrations | `SEC-006`–`SEC-010` | [macOS app lock](../apps/macos/Sources/DayWeaveMac/Store/AppLockController.swift), [Android app lock](../apps/android/app/src/main/java/com/greengolddog/dayweave/security/AppLockController.kt), and their adjacent tests |
| Google OAuth custody and reauthentication foundations, Calendar source import, Google Tasks import, reviewed private-event publication, generated-schedule publication, and crash-safe outbound journals | Parts of `ID`, `GCAL`, `GTASK`, and `SYNC` | [Google service](../server/dayweave-api/src/google_sync), [macOS integration store](../apps/macos/Sources/DayWeaveMac/Store/GoogleIntegrationStore.swift), [Android Google coordinators](../apps/android/app/src/main/java/com/greengolddog/dayweave/sync), and [integration setup](setup-integrations.md) |
| Authenticated Suggestions Inbox, typed proposal preview/apply/undo, private MCP proposal tools, least-privilege client scopes, and fail-closed OAuth account linking | Parts of `AI` and `MCP` | [proposal application service](../server/dayweave-api/src/proposals/application.rs), [MCP tools](../server/dayweave-api/src/mcp/tools.rs), and [proposal contract](proposal-applications.md) |
| Managed Codex App Server login/runtime foundation and planner-aware conversations on macOS, plus authenticated advisory assistant transport on Android | `ID-003`–`ID-004`, parts of `AI-001`–`AI-009` | [macOS Codex controller](../apps/macos/Sources/DayWeaveMac/Store/CodexConversationController.swift), [Android assistant manager](../apps/android/app/src/main/java/com/greengolddog/dayweave/sync/AssistantManager.kt), and [auth rollout](auth-rollout.md) |
| Owner-visible active-device inventory and remote revocation on macOS and Android, with exact session UUID/client-instance/client-kind binding, current-row-derived write authority, memory-only stale-state fencing, confirmed remote revocation with ambiguous-result reconciliation, immediate access/refresh rejection, and transactional 16-session/16-pending-enrollment server bounds | `ID-005`, `SEC-008` | [credential-auth repository](../server/dayweave-api/src/persistence/credential_auth_repository.rs), [PostgreSQL acceptance tests](../server/dayweave-api/tests/credential_auth_postgres.rs), [macOS active-device UI](../apps/macos/Sources/DayWeaveMac/Views/DeviceSessionSettingsView.swift), [Android session manager](../apps/android/app/src/main/java/com/greengolddog/dayweave/sync/DeviceSessionManager.kt), and [auth rollout](auth-rollout.md) |
| Cross-platform account recovery with client-generated 256-bit codes, hash-only server storage, exact issue/rotation/consume replay, one-active-code CAS, full-owner v2 authorization, old-authority fencing, set-based device/enrollment/MCP revocation, successor installation, secure native Keychain/Keystore journals, crash-safe credential/cache handoff, privacy-safe disclosure, and owner-facing recovery controls | `ID-006`, parts of `SEC-004`, `SEC-008`, and `SEC-011` | [recovery migration](../server/dayweave-api/migrations/0028_account_recovery_codes.sql), [PostgreSQL acceptance tests](../server/dayweave-api/tests/credential_auth_postgres.rs), [macOS recovery state](../apps/macos/Sources/DayWeaveMac/Store/DurableAccountRecoveryState.swift), [Android recovery manager](../apps/android/app/src/main/java/com/greengolddog/dayweave/network/AccountRecoveryManager.kt), and [auth rollout](auth-rollout.md) |
| Route-less, default-disabled server foundation for secure personal-account deletion: detached content-free lifecycle/fence evidence, exact transition replay, a 24-hour confirmation boundary, database mutation fencing and anti-resurrection checks, a personal-scope purge primitive, immutable external-principal binding, and provider-cleanup persistence with an exact source manifest, bounded claims/retries, immutable attempt receipts, and current status summaries | Part of `ID-005`, `DATA`, `REL`, and `SEC` | [deletion lifecycle migration](../server/dayweave-api/migrations/0029_account_deletion_lifecycle.sql), [external-principal migration](../server/dayweave-api/migrations/0030_account_deletion_external_principal.sql), [cleanup migration](../server/dayweave-api/migrations/0031_account_deletion_provider_cleanup.sql), [repository](../server/dayweave-api/src/persistence/account_deletion_repository.rs), [integrity tests](../server/dayweave-api/tests/account_deletion_postgres.rs), and [cleanup/upgrade tests](../server/dayweave-api/tests/postgres.rs). Provider calls and credential scrubbing are not implemented; even all-success cleanup and legacy purge paths remain blocked. |
| Google operation admission shared by production OAuth and sync, with committed PostgreSQL ownership across runtimes, sticky persisted closure, nested-call and detached-guardian ownership, cancellation-safe token custody, normal-completion settlement, and exact durable proof plus database recheck before fencing or replay | Part of `ID-005`, `REL`, and `SEC` | [admission controller and tests](../server/dayweave-api/src/provider_admission.rs), [registry migration](../server/dayweave-api/migrations/0032_provider_operation_admission.sql), [registry repository](../server/dayweave-api/src/persistence/provider_admission_repository.rs), [multi-runtime tests](../server/dayweave-api/tests/provider_admission_postgres.rs), [OAuth custody](../server/dayweave-api/src/google_oauth/service.rs), and [fence proof tests](../server/dayweave-api/tests/account_deletion_postgres.rs). Interrupted/ambiguous operations remain retained; authoritative recovery and safe reopening remain open, and deletion is still unavailable. |
| Internal account-deletion preparation with fresh confirmation, pending-only OAuth cancellation and secret scrubbing, bounded existing recovery, exact blocker/retry reporting, atomic readiness-and-closure, and exact committed-closure adoption | Part of `ID-005`, `REL`, and `SEC` | [orchestrator](../server/dayweave-api/src/account_deletion/provider_preparation.rs), [live preparation tests](../server/dayweave-api/src/account_deletion/provider_preparation_tests.rs), [readiness model](../server/dayweave-api/src/account_deletion/provider_readiness.rs), and [database race/authority tests](../server/dayweave-api/tests/account_deletion_postgres.rs). Existing OAuth cleanup may contact Google; this does not implement the deletion grant-revocation worker, recover interrupted registrations, reopen admission, or activate deletion. |
| Native SwiftUI and Jetpack Compose shells with Today/Calendar/Inbox/Assistant navigation, canonical item editors, planning profiles, privacy-first onboarding, and Google review surfaces | Parts of `UX-001`–`UX-012` | [macOS views](../apps/macos/Sources/DayWeaveMac/Views), [Android UI](../apps/android/app/src/main/java/com/greengolddog/dayweave/ui), and platform test suites |
| Native Goals/Projects browsers sourced from the admitted canonical hierarchy and queued changes, including unscheduled outcomes, every lifecycle status, arbitrary-depth traversal, stable sibling order, collapse, local title search, and existing reviewed editing/inspection | Parts of `GOAL-001`, `HIE-001`, and `UX-002`–`UX-004` | [Browser contract](hierarchy-browser.md), [shared projection cases](../fixtures/hierarchy-browser/projection-v1.json), [macOS browser](../apps/macos/Sources/DayWeaveMac/Views/CanonicalHierarchyBrowserView.swift), and [Android browser](../apps/android/app/src/main/java/com/greengolddog/dayweave/ui/screens/CanonicalHierarchyBrowserScreen.kt). Tests cover unbound-cache quarantine, queued topology/privacy, malformed ancestry, deep trees, persisted Android destinations, and batch privacy parity. Project authoring, nested capture and independent progress components are covered in separate slices; parent auto-completion and weekly allocations remain unfinished. |
| Native Project creation/replacement, typed date-only and date-time hard/soft deadlines, independent leaf-effort controls, and reviewed New Goal/New Project/Add Subtask capture on both clients | `DOM-001`, `DOM-006`, parts of `HIE-001`–`HIE-002` and `GOAL-001` | [Structural authoring contract](structural-authoring.md), [shared request cases](../fixtures/structural-authoring/requests-v1.json), [HTTP/PostgreSQL tests](../server/dayweave-api/tests/structural_items_api.rs), [macOS structural tests](../apps/macos/Tests/DayWeaveMacTests/CanonicalStructuralAuthoringTests.swift), and [Android structural tests](../apps/android/app/src/test/java/com/greengolddog/dayweave/model/CanonicalStructuralAuthoringTest.kt). Independent request markers preserve legacy retry bodies across strict macOS V25 / Android Room-JSON V21 migration, cleanup, and restart. Selected ancestry is revalidated; bound requests remain immutable, privacy review stays protected, and imported/unsupported bodies remain read-only. |
| Health Connect energy-signal provider on Android with explicit permissions, private projection, and manual-safe fallback boundaries | `CTX-006`, future boundary for `CTX-008` | [Health Connect integration](../apps/android/app/src/main/java/com/greengolddog/dayweave/health) |
| Reproducible local macOS and signed-APK build paths, guarded signing boundaries, CI security scans, container builds, encrypted backup scripts, and a cost-guarded Nebius Terraform plan | Parts of `OPS`, `SEC-004`, `SEC-011`, and `DATA-003`–`DATA-006` | [release workflow](../.github/workflows/release.yml), [local build scripts](../scripts), and [deployment assets](../deploy) |
| Shared habit recurrence and lifecycle core: bounded custom rules, rolling and completion-relative cadence, stable occurrence identity and move proof, missed/spacing policies, corrections, partial-progress demand, pauses, streaks, analytics, and scheduler-helper budgets; plus strict Android habit API transport | Core of `HAB-001`–`HAB-009`, parts of `CON` and `SCH` | [habit domain](../crates/dayweave-core/src/habits.rs), [recurrence engine](../crates/dayweave-core/src/recurrence.rs), [custom recurrence](../crates/dayweave-core/src/custom_recurrence.rs), [scheduler tests](../crates/dayweave-core/tests/scheduler.rs), and [habit ledger design](habit-occurrence-ledger.md) |
| Authoritative habit backend with published-schedule evidence admission, immutable PostgreSQL outcome/pause/missed-resolution history, server-clock skip/carry/reduce/ask reconciliation, current-publication binding, strict idempotent replay, ordered delta and content-free invalidation, private analytics, and lifecycle hydration that rejects spoofed, obsolete, or non-leaf authority | Server slice of `HAB-006`–`HAB-009`, `SYNC`, and `SEC` | [habit service](../server/dayweave-api/src/habits), [PostgreSQL repository](../server/dayweave-api/src/persistence/habit_repository.rs), [ledger migration 0026](../server/dayweave-api/migrations/0026_habit_occurrence_ledger.sql), [missed-resolution migration 0027](../server/dayweave-api/migrations/0027_habit_missed_resolutions.sql), [API tests](../server/dayweave-api/tests/habits_api.rs), and [PostgreSQL tests](../server/dayweave-api/tests/habits_postgres.rs) |
| macOS habit occurrence, missed-review, and statistics experience with strict transport, encrypted origin-bound offline persistence, exact replay leases and response binding, conflict review, independent outcome/resolution coordinates, terminal delta authority, done/partial/skipped corrections, skip/carry/reduce actions, pause/resume, trends, streaks, quantities, content-free invalidation/catch-up, habit-aware local composition, and privacy scrubbing | Client slice of `HAB-006`–`HAB-009`, parts of `SYNC`, `SEC`, `SCH`, and `UX` | [habit models](../apps/macos/Sources/DayWeaveMac/Models/HabitModels.swift), [habit sync store](../apps/macos/Sources/DayWeaveMac/Store/HabitSyncStore.swift), [canonical composition store](../apps/macos/Sources/DayWeaveMac/Store/CanonicalSyncStore.swift), [habit UI](../apps/macos/Sources/DayWeaveMac/Views/PlanningViews.swift), and adjacent tests |
| Android habit occurrence, missed-review, and statistics experience with strict origin-bound transport, an encrypted Room offline ledger, exact replay leases and reviewed failures, independent outcome/resolution coordinates, terminal delta checkpoints, content-free invalidation with polling fallback, durability-first done/partial/skipped corrections, skip/carry/reduce actions, pause/resume, bounded analytics, correction-safe retention, current-publication occurrence fencing, habit-aware local composition, and finite custom-RRULE authoring | Client slice of `HAB-006`–`HAB-009`, parts of `SYNC`, `SEC`, `SCH`, and `UX` | [habit models](../apps/android/app/src/main/java/com/greengolddog/dayweave/model/HabitModels.kt), [habit sync manager](../apps/android/app/src/main/java/com/greengolddog/dayweave/sync/HabitSyncManager.kt), [habit UI](../apps/android/app/src/main/java/com/greengolddog/dayweave/ui/screens/HabitSections.kt), [Room V19 migration test](../apps/android/app/src/androidTest/java/com/greengolddog/dayweave/HabitMissedResolutionMigrationTest.kt), [Room V20 migration test](../apps/android/app/src/androidTest/java/com/greengolddog/dayweave/PublishedOccurrenceMembershipMigrationTest.kt), and adjacent unit tests |

## Active implementation

The current milestone is full-product closure after the cross-platform habit
core. Goals/Projects now browse the full admitted item tree on both clients,
including work with no schedule blocks. Shared fixtures and native regression
tests cover navigation projection and conservative privacy/editing boundaries;
controlled client/service and owner-device acceptance remain open.
Required-descendant completion now has a shared iterative engine and a versioned
one-off server contract. Required/optional branches, manual-policy modes and
exact reopening provenance are applied synchronously by direct, proposal and
Google canonical writers, including startup reconciliation and durable replay.
The server checkpoint passed 626 tests, including deep cascades, proposal undo
and provider mapping freshness. Both native clients now have required-edge and
manual-policy review, encrypted exact intent, restart/conflict recovery and
policy-qualified parent selection. Full native regression/build gates pass;
historical receipts settle intent without replacing current items, and editing
waits for durable terminal catch-up. Version 1 has no subtree-privacy witness,
so completion-derived review and intent always remain protected. Bounded
current-state bootstrap is separately covered. A
[nine-phase controlled completion run](native-completion-convergence.md) now
verifies both native stores against one real service: stale-policy review,
lost-reply/restart replay, durable catch-up, required/optional completion and
native child creation that reopens the exact ancestry. Its final independent
SQL audit and owned cleanup passed. This four-to-five-node scenario does not
close deep native cascade/lifecycle timing, Android instrumented interaction,
qualified recurring instances, production device-auth/TLS or owner acceptance.
See [parent completion](hierarchy-completion.md) for the exact boundary.
The recurring-instance work now has a verified depth-safe scheduling
prerequisite: the shared engine/helper and server composition can process a
controlled 5,000-node routine tree without a fixed logical-depth gate or
duplicated parent demand, while retaining item, byte and work budgets.
Habit ownership/carry probes also check those budgets before raw expansion.
The separate [occurrence-member server checkpoint](routine-occurrence-completion.md)
now adds immutable publication admission, independent lifecycle custody, reviewed
member commands, whole-instance delta/paging, and planning/publication/execution
fences. Native step actions, encrypted replicas/exact outboxes and cross-device
recovery remain unfinished, along with cadence and semantic rebase workflows.
Existing broader completion and hierarchy estimates remain unchanged; this
in-progress subfeature has its own approximate scope estimate below.
Project and typed structural authoring now use the existing encrypted journals on
both clients, with New Goal/New Project/Add Subtask entry points and explicit
review before saving. Date-only and soft deadlines retain their full meaning,
and own effort contributes only for executable leaves. Strict request-version
migration and frozen bound requests keep older offline work recoverable. See
[structural authoring](structural-authoring.md) for the exact boundary and gates.
Read-only recorded leaf progress and effort summaries now appear on both native
clients. The shared canonical-second reducer, parity fixtures, native hydration,
privacy, pending-change admission, and cached presentation pass the full core
and native gates plus synthetic native UI checks. These totals do not mutate
parent lifecycle or replace independent progress and automatic-completion
flows. Controlled refresh/offline convergence and owner
acceptance remain open. See [hierarchy progress](hierarchy-progress.md).
Independent percentage, self-reported time, and exact named quantities are now
implemented as a separate revisioned sidecar. The backend has concurrent CAS,
historical replay, active-execution noninterference and deletion-fence coverage;
both native editors, strict transports, encrypted upgrades and replay/privacy
paths pass their full regression gates and synthetic native checks.
This also permits a parent's own components without replacing child totals.
The [controlled native convergence gate](native-progress-convergence.md) now
passes all six macOS/Android JVM phases against one disposable HTTP/PostgreSQL
service, including competing offline edits, a lost successful reply, reviewed
conflict resolution and exact historical replay after restart. SQL checks prove
two successful receipts, one sidecar and unchanged canonical/child state.
Foreground/detail polling, reconnect wakeups, independent outbox replay,
foreground/credential lifetime fences, frozen editor input and per-item canonical
catch-up now have deterministic native coverage. This does not establish physical
OS-network/foreground timing, production device authentication/TLS or owner-device
interaction acceptance.
See [independent progress](item-progress.md) for the wire contract and remaining
verification. Automatic lifecycle completion is not part of this checkpoint.
Account-recovery codes are implemented across the authoritative backend
and both native clients, with controlled service integration and owner-device
acceptance still open. Work has started on secure account deletion: the
route-less server/database foundation remains default-disabled, and its fenced
personal-scope invariants have automated PostgreSQL coverage. Each new lifecycle
is pinned to the exact versioned deployment-keyed external principal derived for
the configured owner. The provider-cleanup persistence/claim substrate now seals
an immutable source manifest, bounds attempts and leases, preserves exact
historical result replay, and exposes current progress counts. It makes no
provider calls and leaves source credentials behind the fence, including after
recorded success. Production Google services share admission backed by a durable
PostgreSQL registry across runtimes and retain ownership through nested work and
detached token custody. Interrupted/ambiguous registrations remain unresolved;
normal completions settle exactly. Fencing requires a durable controller's
drained proof and a transaction-time registry recheck. Internal bounded
[pre-close orchestration](../server/dayweave-api/src/account_deletion/provider_preparation.rs)
now validates fresh deletion authority, cancels only pending OAuth sessions with
verifier/authorization-URL scrubbing, and runs existing admitted cleanup and
staged reconciliation without bypassing backoff or retained-grant protections.
That existing cleanup can call Google; it is separate from the still-absent full
deletion grant-revocation worker. A complete content-free readiness snapshot and
sticky closure share one exclusive-barrier transaction, with exact committed
closure adoption on retry and no reopening. The internal preparation-to-fence
path and blocked-state behavior have live PostgreSQL coverage.
Authoritative interrupted-runtime recovery, safe reopening, Google grant
identity proof, the exclusive
runtime-held external restore permit, HTTP/native workflows, and verified backup
erasure remain open. PostgreSQL and Rust still block purge.

Every feature slice whose status is **In progress** has its own approximate
completion value below. These are evidence-based scope estimates, not elapsed
time or delivery forecasts. A percentage advances only after a material code,
integration, or verification checkpoint; the notes state what prevents 100%.

| In-progress feature slice | Approx. complete | Evidence and next checkpoint |
| --- | ---: | --- |
| Independent recurring-instance member completion | **50%** | Server/shared authority and strict native transports are followed by verified encrypted per-instance state/outboxes, exact receipt settlement, bounded terminal installation and migration preserving existing work. The separate helper v2 can consume explicitly supplied lifecycle evidence. Protected review and live replay/catch-up coordinators, authenticated current-source evidence, native local-composition/publication fences and first-admission convergence are not yet integrated. Completion-relative cadence with unfinished optional-instance retention, semantic template rebase, qualified execution credit, nested recurrence, step-specific defer and device acceptance remain open. This is a coarse scope estimate, not a remaining-time forecast. |
| Required-descendant parent completion and manual overrides | **70%** | The shared engine and one-off server writers have protected native review/override controls, strict wire, encrypted exact intent and qualified parent attachment. A nine-phase controlled macOS/Android JVM run now proves real policy conflicts, lost-reply/restart recovery, historical receipt replay without rollback, durable terminal catch-up, optional branches, completion cascades and native child creation that reopens the exact ancestry; independent SQL confirms immutable custody. This advance closes a bounded four-to-five-node service convergence gate. Close deep native cascade and lifecycle timing, instrumented Android interaction, qualified recurring-instance integration, production device-auth/TLS and owner acceptance. The ten Android UI tests and Room migration test remain compiled only; this is not full-feature completion. |
| Independent percentage, time, and named-unit progress | **95%** | Separate item-scoped authority, immutable replay/audit, both native editors and encrypted upgrades pass shared exact-value fixtures, Rust/PostgreSQL and native regression/build gates. Six controlled macOS/Android JVM phases prove convergence through competing offline edits, genuine conflict/re-review, a lost reply and service/native restart; SQL and native checks preserve canonical state and child progress. Foreground/detail/reconnect integration now has deterministic ownership, cancellation, backoff, stale-editor and canonical-catch-up tests. Close physical-device lifecycle/OS-network timing, production TLS/device-auth integration and owner acceptance. Parent own components remain separate from descendant counts; execution reconciliation, required-component policy and automatic completion are separate unfinished scope. |
| Canonical Goals/Projects hierarchy browsing | **90%** | Both native clients show unscheduled goals and complete scoped descendant trees, retain terminal statuses, support local search/collapse and existing exact review routes, and quarantine unbound legacy data. Batch privacy/dependency lookups and graph-keyed source reuse avoid per-row full-graph rebuilds; malformed ancestry remains protected and read-only across browser and Inbox. Close controlled two-client/service refresh, native interaction/accessibility acceptance, and owner-device checks. Parent auto-completion and weekly allocations remain unfinished; independent progress components and project authoring have separate checkpoints. |
| Native project, typed-deadline, and nested-item authoring | **90%** | Both native editors support Project and complete deadline/own-effort round-trips, ordinary hierarchy capture, strict civil-date boundaries, selected-path parent admission, sticky sensitive review, and independent legacy/modern request replay. Shared HTTP fixtures, encrypted migration tests, native regression/build gates, and synthetic UI checks cover the slice. The completion gate now verifies Android child creation under a qualified Completed parent and macOS hydration of the reopened ancestry. Close wider controlled creation, replacement, reparenting, and offline convergence plus owner-device interaction acceptance; independent parent progress and auto-completion remain separate scope. |
| Recorded hierarchy leaf-progress and effort summaries | **90%** | Both native browsers/details use admitted complete-cache input, exact-second rich estimates, separate lifecycle/recurring/event counts, deep iterative aggregation, pending-change withholding, descendant privacy, and search-independent caches. Seven valid/fifteen invalid shared fixtures, full core/native regression/build gates, synthetic macOS rendering, and ten Android UI checks pass. Close controlled refresh/offline convergence and owner-device acceptance. Independent progress components have their own checkpoint; required-child completion, weighted aggregation and manual overrides remain unfinished. |
| Leaf-only hierarchy scheduling and duration roll-ups | **90%** | Shared-core placement, recurrence, pinned/reservation validation, deep roll-ups, and native readiness/execution guards follow the recorded leaf-only rule. Canonical child relationships survive Inbox/rejected-item omission from planning. Own-effort flags cannot schedule parents alongside children, and historical progress/encrypted anchor designations survive topology changes. Close a controlled two-client/service hierarchy-change run and native UI acceptance; hierarchy navigation and independent parent components have separate checkpoints and acceptance gates. |
| Cross-platform account-recovery code flow | **90%** | The independently reviewed server, macOS, and Android implementations cover exact replay, secure journals, crash-safe authority replacement, cache quarantine, successor acknowledgement, privacy clearing, and strict transport validation; close a controlled two-client/service recovery rehearsal and owner-device UI acceptance. |
| Secure account-deletion lifecycle | **41%** | The route-less foundation has exact cleanup manifests and bounded claims/retries, plus PostgreSQL admission ownership across runtimes. Provider work starts only after registration commits; interrupted/ambiguous records never auto-expire, and fencing requires exact durable proof plus a transaction-time registry recheck. Bounded pre-close orchestration validates fresh authority, cancels only pending OAuth authorizations, runs existing guarded cleanup/reconciliation, and atomically verifies readiness before sticky closure; live tests cover preparation through accepted fence proof, closure races, invalid/reentrant authority, and already-closed unfinished work. The full deletion grant-revocation worker and source credential scrubbing remain absent, and all-success/legacy purge stays blocked. Next complete authoritative interrupted-runtime recovery and safe reopening; then Google project-and-subject grant proof, the deletion worker with credential scrubbing, the external runtime-held restore permit, credential-only HTTP/native teardown, least-privilege roles, and backup-expiry evidence. |
| Native active-device inventory and session revocation | **95%** | The bounded PostgreSQL contract and both native account surfaces have strict identity, scope, privacy, stale-state, and proof-based ambiguous-result recovery for remote and current-device revocation; close a controlled two-client/service run plus Android instrumented and owner-device UI acceptance. |
| Native rich duration shape and provenance | **95%** | Exact, ranged, and unknown durations, provenance, rollback-safe persistence, and request replay are covered by the full macOS and Android gates; close the controlled native/service contract run and device acceptance. |
| General habit minimum spacing | **95%** | Both native authoring paths and every shared recurrence family enforce the stricter applicable floor with explicit unmet demand; close the controlled native/service run and device acceptance. |
| Missed-occurrence skip/carry/reduce/ask workflow | **90%** | Durable server-clock reconciliation, cancellation/restoration, current-publication scheduling effects, encrypted offline replay, migration hardening, and native review/action surfaces pass the Rust/PostgreSQL, macOS, and Android automated gates; close controlled native/service convergence and owner-device acceptance. |
| Controlled native-client/service habit integration | **35%** | The real HTTP/PostgreSQL service path is covered independently and native transports have contract coverage; run both native clients against the same controlled service and prove convergence. |
| Full habit lifecycle on macOS and Android | **75%** | Occurrence, correction, missed-policy, pause, analytics, invalidation, offline replay, conflict, migration, retention, and recomposition behavior has broad automated coverage; close the controlled-service and native UI acceptance matrix. |
| Final macOS build and Pixel 11 APK verification | **45%** | Reproducible local-build and signed-APK paths exist; finish release gates, produce final artifacts, install them, and verify them on the owner's devices. |

The following remaining checkpoints are not yet marked in progress:

- complete the required seven-consecutive-day trial, fix every critical or
  major issue, and rerun affected acceptance paths.

These items remain **in progress** until the integrated client/service gates,
controlled device checks, and real-life acceptance trial are complete.

## Features still to add or close

Every requirement remains mandatory unless the owner explicitly changes
[product-requirements.md](product-requirements.md). This table summarizes the
remaining work without pretending that partially implemented areas are done.

“Approx. complete” is a coarse, evidence-based estimate of implemented agreed
scope. It is not elapsed-time progress, remaining effort, or a delivery promise;
areas differ greatly in size and the values must not be averaged into an app-wide
percentage.

| Product area | Status | Approx. complete | Work still required for full acceptance |
| --- | --- | ---: | --- |
| Identity and accounts (`ID`) | **In progress** | **77%** | Close controlled active-device, recovery, and credential-only cutover acceptance; finish integration disconnect/reconnect parity, the still-disabled account-deletion workflow and external safety gates, and final managed Codex login verification on both platforms. |
| Common item model (`DOM`) | **In progress** | **80%** | Close independent-progress physical-device lifecycle/timing and native acceptance; complete the remaining rich fields, templates, bulk operations, audit/undo presentation, and cross-platform editing paths. |
| Hierarchy, goals, projects, routines, dependencies (`HIE`, `GOAL`, `ROU`) | **In progress** | **69%** | One-off parent completion now has synchronous server integration, protected native review, encrypted recovery, qualified parent attachment and verified bounded two-client/service conflict/cascade/reopening convergence. Close deep native cascade/lifecycle timing, instrumented interaction, qualified recurrence and owner acceptance, plus wider controlled project/nested-authoring and recorded-summary convergence, independent-progress physical-device timing and hierarchy interaction acceptance; finish milestones/measures, weekly goal allocation, routine authoring/execution and complete dependency conflict explanations. |
| Scheduling restrictions and profiles (`CON`) | **In progress** | **65%** | Close the remaining per-item hard/soft restrictions, buffers, caps, pinning, partial-work accounting, acknowledged overrides, and profile precedence cases. |
| Scheduler intelligence (`SCH`) | **In progress** | **55%** | Complete configurable objective ordering, movement-cost tuning, alternatives/what-if, overload resolution, fragile-deadline warnings, “Why here?”, learned preference controls, and 90-day performance proof. |
| Habits (`HAB`) | **In progress** | **90%** | Run native-client/service E2E, verify the local macOS build and Pixel 11 APK, close the native UI acceptance matrix, and finish the seven-day trial. |
| Active execution (`EXE`) | **In progress** | **65%** | Add Focus/DND mappings, inactivity correction, duration-learning controls, Pomodoro/mandatory-break settings, and full cross-device/UI coverage. |
| Google Calendar (`GCAL`) | **In progress** | **45%** | Complete full bidirectional parity for series scopes, attendees/RSVP, conferencing, attachments, flexible-event moves, birthdays/observances, OOO/free/tentative policy, conflicts, travel, density, and notification ownership. |
| Google Tasks (`GTASK`) | **In progress** | **45%** | Finish bidirectional field/list parity, external completion/deletion/due-date reactions, shared conflict/undo behavior, and controlled real-account tests. |
| Capture, Inbox, files, and search (`CAP`, `SEA`) | **In progress** | **25%** | Add voice, global/menu/share/shortcut/drag capture, attachment storage/OCR, URL snapshots, duplicate review, full-text/history search, and privacy-preserving semantic search. |
| Embedded assistant (`AI`) | **In progress** | **40%** | Complete universal and goal/project chats, all-item natural-language operations, overload/review workflows, visible memory/model/privacy controls, sourced web search, grounded explanations, offline queueing, and proactive limits. |
| External MCP, Codex skill, Suggestions Inbox (`MCP`) | **In progress** | **55%** | Complete permission configuration UI, all proposal kinds, proposal editing/bulk handling/expiry, per-client revocation, conversation continuation, and end-to-end ChatGPT/Codex verification. |
| Offline synchronization and conflicts (`SYNC`) | **In progress** | **58%** | Extend the proven journal/invalidation/replay-lease pattern to every entity, finish field-level conflict UI and safe synchronized undo, and meet the ten-second convergence target. |
| Time, travel, location, health, weather (`CTX`) | **In progress** | **25%** | Add travel-zone profiles, absolute/floating time UX, Maps travel modes, location/geofences, manual energy correction, weather suggestions, and the planned WHOOP provider extension. |
| Notifications and platform integration (`NOT`) | **In progress** | **20%** | Complete synchronized notification actions, privacy-safe lock presentation, macOS menu bar/widgets/Spotlight/Shortcuts/share/login helper, Android timer notification/tile/widgets/share/shortcuts/actions, and Focus/DND mappings. |
| Client polish and accessibility (`UX`) | **In progress** | **40%** | Finish remaining primary views and More destinations, adaptive inspector behavior, theming, timeline zoom, dim/hide completed work, drag/resize/pin/multi-select, command palette/shortcuts, accessibility, and demo workspace; close Goals/Projects device and interaction acceptance. |
| Export, backup, and recovery (`DATA`) | **In progress** | **30%** | Add encrypted full backup plus JSON/CSV/ICS/Markdown export, attachment object storage, production-shaped migration checks, and timed restore/RPO/RTO evidence. |
| Security and privacy (`SEC`) | **In progress** | **70%** | Finish production key separation and envelope encryption, remaining sensitive-item boundaries, external-client permission/revocation UX and live OAuth review, telemetry controls, ingress/per-principal rate limits and suspicious-authentication alerts, automatic maintenance/runtime scanning, alert delivery, and full adversarial security acceptance. |
| Operations and distribution (`OPS`) | **In progress** | **40%** | Provision only after approval, configure private HTTPS/monitoring/alerts, exercise dev/beta/stable release and rollback paths, and produce final local macOS and signed Android artifacts with provenance. |
| Performance, reliability, and complete verification (`PERF`, `REL`, `TEST`) | **Planned** | — | Run all explicit launch/UI/scheduler/sync budgets, property and provider suites, production-shaped migrations, destructive restore rehearsal, security tests, and complete end-to-end acceptance. |

## External verification gates

These are not permission to spend money or use private data. They are tracked
separately because final acceptance needs owner-controlled resources:

- **External gate:** approval before creating or changing paid Nebius resources;
- **External gate:** Google Cloud credentials plus isolated Calendar and Tasks
  test data before any controlled real-account check;
- **External gate:** installation and acceptance testing on the owner's Pixel 11;
- **External gate:** owner verification of the final local macOS build;
- **External gate:** the required seven-consecutive-day real-life trial, followed
  by fixes and retesting of every critical or major issue.

## Maintenance rules

1. A feature commit must update this ledger when it changes the truth of any
   row. Status moves only after the corresponding tests or guarded build pass.
2. New requirements are added first to the discovery/requirements source of
   truth, then reflected here. This ledger never silently drops scope.
3. “Implemented + covered” describes only the exact slice stated; full product
   completion is governed by the definition of complete in
   [product-requirements.md](product-requirements.md#9-definition-of-complete).
4. The date and tracking baseline are refreshed whenever status changes.
5. Credentials, tokens, private endpoints, tenant identifiers, signing keys,
   personal calendar content, and other private data must never be recorded in
   this public ledger.
6. Approximate percentages change only when implementation or agreed scope
   materially changes. They describe scope completeness, never elapsed time or
   a delivery commitment, and do not trigger unsolicited chat estimates.
7. Every individual feature slice marked **In progress** must have its own
   approximate completion percentage and a concrete next checkpoint. Broad
   product-area percentages remain separate and are never calculated by simply
   averaging feature rows.
